# Frontend production configuration. Copy to frontend/.env.production.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_KEYCLOAK_URL=
NEXT_PUBLIC_KEYCLOAK_REALM=portfolio-builder
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=portfolio-frontend

# Internal only: use the private service hostname, never a browser-visible URL.
BACKEND_INTERNAL_URL=http://backend:8081
