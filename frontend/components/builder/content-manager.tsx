"use client";
import Image from "next/image"; import { useEffect, useState } from "react"; import { Plus, Trash2, Upload } from "lucide-react"; import { assetApi } from "@/features/content/assets"; import { contentApi } from "@/features/content/api"; import type { Education, Experience, Project, Skill, SocialLink } from "@/features/content/types";
type Kind="experiences"|"educations"|"skills"|"projects"|"social-links"; const labels:Record<Kind,string>={experiences:"Experience",educations:"Education",skills:"Skills",projects:"Projects","social-links":"Social Links"};
export function ContentManager({kind}:{kind:Kind}){const [items,setItems]=useState<any[]>([]);const [open,setOpen]=useState(false);const [saving,setSaving]=useState(false);const [error,setError]=useState("");const load=()=>contentApi.list<any>(`/api/${kind}`).then(setItems).catch(()=>setError("Unable to load this section."));useEffect(()=>{void load();},[kind]);async function submit(data:any){setSaving(true);setError("");try{const created=await contentApi.create<any>(`/api/${kind}`,data);setItems([...items,created]);setOpen(false);}catch(e){setError(e instanceof Error?e.message:"Unable to save.");}finally{setSaving(false)}}async function remove(id:string){if(!window.confirm("Delete this item?"))return;try{await contentApi.remove(`/api/${kind}`,id);setItems(items.filter(x=>x.id!==id));}catch{setError("Unable to delete this item.")}}return <section className="rounded-xl border border-gray-200 bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">{labels[kind]}</h2><p className="mt-1 text-sm text-muted">{items.length?`Manage your ${labels[kind].toLowerCase()}.`:`No ${labels[kind].toLowerCase()} added yet.`}</p></div><button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-white"><Plus size={16}/> Add {labels[kind].replace("s","")}</button></div>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-6 space-y-3">{items.length===0?<div className="rounded-lg bg-surface p-6 text-sm text-muted">Add content to make your portfolio more complete.</div>:items.map(item=><article key={item.id} className="flex items-start justify-between rounded-lg border border-gray-100 p-4"><div><p className="font-semibold">{item.title||item.company||item.school||item.name||item.platform}</p><p className="mt-1 text-sm text-muted">{item.position||item.degree||item.shortDescription||item.category||item.url}</p>{item.technologies&&<p className="mt-2 text-xs text-primary">{item.technologies.join(" · ")}</p>}{kind==="projects"&&<ProjectImage item={item} onChange={load} onError={setError}/>}</div><button aria-label="Delete item" onClick={()=>remove(item.id)} className="rounded p-2 text-red-600 hover:bg-red-50"><Trash2 size={16}/></button></article>)}</div>{open&&<EntryForm kind={kind} saving={saving} onCancel={()=>setOpen(false)} onSubmit={submit}/>}</section>}
function ProjectImage({item,onChange,onError}:{item:any;onChange:()=>void;onError:(value:string)=>void}){const [busy,setBusy]=useState(false);async function upload(file?:File){if(!file)return;if(!["image/jpeg","image/png","image/webp"].includes(file.type))return onError("Only JPEG, PNG, and WebP images are supported.");setBusy(true);try{await assetApi.uploadProjectImage(item.id,file);onChange()}catch(e){onError(e instanceof Error?e.message:"Unable to upload project image.")}finally{setBusy(false)}}async function remove(){setBusy(true);try{await assetApi.deleteProjectImage(item.id);onChange()}catch{onError("Unable to remove project image.")}finally{setBusy(false)}}return <div className="mt-3 flex flex-wrap items-center gap-3">{item.thumbnailUrl&&<Image src={item.thumbnailUrl} alt="Project thumbnail" width={96} height={54} className="aspect-video rounded object-cover"/>}<label className="inline-flex cursor-pointer items-center gap-1 rounded border px-2 py-1 text-xs"><Upload size={13}/>{busy?"Uploading…":"Upload Image"}<input className="hidden" disabled={busy} type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>void upload(e.target.files?.[0])}/></label>{item.thumbnailUrl&&<button disabled={busy} onClick={()=>void remove()} className="text-xs text-red-600">Remove Image</button>}</div>}
function EntryForm({kind,saving,onCancel,onSubmit}:{kind:Kind;saving:boolean;onCancel:()=>void;onSubmit:(v:any)=>void}){const [values,setValues]=useState<any>({currentlyWorking:false,featured:false,technologies:""});const field=(name:string,label:string,type="text",required=false)=><label className="block text-sm font-medium">{label}<input required={required} type={type} value={values[name]??""} onChange={e=>setValues({...values,[name]:e.target.value})} className="mt-1 w-full rounded border border-gray-200 px-3 py-2"/></label>;function submit(e:React.FormEvent){e.preventDefault();const v={...values,currentlyWorking:!!values.currentlyWorking,featured:!!values.featured,technologies:values.technologies?values.technologies.split(",").map((x:string)=>x.trim()).filter(Boolean):[]};onSubmit(v)}return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-lg overflow-auto rounded-xl bg-white p-6"><h3 className="text-lg font-bold">Add {labels[kind].replace("s","")}</h3><div className="mt-5 grid gap-3">{kind==="experiences"&&<>{field("company","Company", "text",true)}{field("position","Position","text",true)}{field("location","Location")}{field("startDate","Start date","date",true)}{field("endDate","End date","date")}<label className="flex gap-2 text-sm"><input type="checkbox" checked={values.currentlyWorking} onChange={e=>setValues({...values,currentlyWorking:e.target.checked,endDate:""})}/> Currently working</label></>}{kind==="educations"&&<>{field("school","School","text",true)}{field("degree","Degree")}{field("major","Major")}{field("startDate","Start date","date")}{field("endDate","End date","date")}</>}{kind==="skills"&&<>{field("name","Skill name","text",true)}{field("category","Category")}</>}{kind==="projects"&&<>{field("title","Title","text",true)}{field("shortDescription","Short description")}{field("githubUrl","GitHub URL","url")}{field("demoUrl","Demo URL","url")}{field("technologies","Technologies (comma separated)")}<label className="flex gap-2 text-sm"><input type="checkbox" checked={values.featured} onChange={e=>setValues({...values,featured:e.target.checked})}/> Featured</label></>}{kind==="social-links"&&<><label className="text-sm font-medium">Platform<select required value={values.platform??""} onChange={e=>setValues({...values,platform:e.target.value})} className="mt-1 w-full rounded border border-gray-200 px-3 py-2"><option value="">Choose platform</option>{["GITHUB","LINKEDIN","FACEBOOK","INSTAGRAM","TELEGRAM","YOUTUBE","X","WEBSITE"].map(x=><option key={x}>{x}</option>)}</select></label>{field("url","URL","url",true)}</>}</div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onCancel} className="rounded border px-3 py-2">Cancel</button><button disabled={saving} className="rounded bg-primary px-3 py-2 text-white">{saving?"Saving…":"Save"}</button></div></form></div>}
