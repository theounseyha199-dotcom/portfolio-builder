"use client";
import { createContext, useContext, type HTMLAttributes, type ReactNode } from "react"; import { Controller, FormProvider, useFormContext, type ControllerProps, type FieldValues, type Path } from "react-hook-form"; import { Label } from "./label";
const FieldContext=createContext<{name:string}>({name:""}); export const Form=FormProvider;
export function FormField<T extends FieldValues>(props:ControllerProps<T>){return <FieldContext.Provider value={{name:props.name}}><Controller {...props}/></FieldContext.Provider>;} export function FormItem({children,...props}:HTMLAttributes<HTMLDivElement>){return <div {...props}>{children}</div>;} export function FormLabel({children}: {children:ReactNode}){return <Label>{children}</Label>;} export function FormControl({children}:{children:ReactNode}){return <>{children}</>;} export function FormDescription({children}:{children:ReactNode}){return <p className="text-sm text-muted">{children}</p>;} export function FormMessage(){const {name}=useContext(FieldContext);const {formState}=useFormContext();const message=formState.errors[name as Path<FieldValues>]?.message;return message?<p className="text-sm text-red-600">{String(message)}</p>:null;}
