package com.portfolio.features.experience.repository; import com.portfolio.features.experience.entity.Experience; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface ExperienceRepository extends JpaRepository<Experience,UUID>{ List<Experience> findByPortfolioIdOrderBySortOrderAscStartDateDesc(UUID portfolioId); boolean existsByPortfolioId(UUID portfolioId); }
