package com.portfolio.features.social.repository; import com.portfolio.features.social.entity.SocialLink; import java.util.*; import org.springframework.data.jpa.repository.*; public interface SocialLinkRepository extends JpaRepository<SocialLink,UUID>{List<SocialLink> findByPortfolioIdOrderBySortOrderAsc(UUID id); boolean existsByPortfolioId(UUID id); boolean existsByPortfolioIdAndPlatformAndIdNot(UUID p,String platform,UUID id); boolean existsByPortfolioIdAndPlatform(UUID p,String platform);}
