package com.portfolio.features.education.repository; import com.portfolio.features.education.entity.Education; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface EducationRepository extends JpaRepository<Education,UUID>{ List<Education> findByPortfolioIdOrderBySortOrderAscStartDateDesc(UUID id); boolean existsByPortfolioId(UUID id); }
