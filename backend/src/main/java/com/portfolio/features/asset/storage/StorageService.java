package com.portfolio.features.asset.storage; import java.io.InputStream; import org.springframework.web.multipart.MultipartFile; public interface StorageService {StoredFile upload(MultipartFile file,String directory);void delete(String objectKey);String getPublicUrl(String objectKey);InputStream open(String objectKey);}
