package com.portfolio.features.asset.repository; import com.portfolio.features.asset.entity.PortfolioAsset; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface PortfolioAssetRepository extends JpaRepository<PortfolioAsset,UUID>{Optional<PortfolioAsset> findByPortfolioIdAndAssetType(UUID id,String type);Optional<PortfolioAsset> findByProjectIdAndAssetType(UUID id,String type);Optional<PortfolioAsset> findByObjectKey(String objectKey);}
